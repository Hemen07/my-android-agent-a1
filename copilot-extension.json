{
  "schema_version": "V1.A1",
  "name": "AndroidAgent-A1",
  "description": "Android Custom Agent A1 - Development assistant",
  "agent": {
    "endpoint": "custom",
    "commands": [
      {
        "name": "cmdCI",
        "description": "Clean up unused imports in Kotlin files",
        "system_prompt": "Remove all unused imports from the selected Kotlin code and optimize the import statements"
      }
    ]
  }
}
